A faire pour le parsing:

    - S'occuper du parsing des redirections (Un peu chiant ca);

    - Ne pas splitter en fonctions des quotes.
        Exemple: "wshlamiff"'cava?' -> ca va dans le meme token (J ai oublier de checker ca xD)
    
    -Remplacer les $MACHINS par la valeur de la variable; (C est en cours mais galere)
    
    -Supprimer les quotes

    -Typer les tokens pour savoir leur contenu (Partiellement fait)

    -Creer les fichiers pour les redirections (Ca c est le plus chiant du parsing et j ai pas envie )

    -Checker les leaks et les retours (La variable globale g_return_value)


A faire pour l execution:

    -TOUT MDRRRR Bon courage mon reuf xD